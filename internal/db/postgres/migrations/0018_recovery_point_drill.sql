-- HAUSV-562: no schema change. This file exists so that release 1.3.4 takes
-- the schema path on the live host and proves the PostgreSQL half of the
-- pre-deploy recovery point with a real release. A drill, on purpose.
SELECT 1;
