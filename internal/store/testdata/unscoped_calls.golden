cmd/hausv-org/migrate_data.go	runMigrateData	literal	data mover: copies every tenant's rows from SQLite into an empty PostgreSQL target before the cutover; there is no request and no tenant in scope, and a fail-closed policy would otherwise refuse every insert
internal/dbtest/dbtest.go	MaintenanceView	literal	test fixtures and assertions read and write outside every tenant lane
internal/energy/storage.go	(*SQLStore).PurgeExpired	literal	retention sweep over every tenant's expired energy imports, intervals and assessments: the boot path that calls it has no tenant in scope
internal/energy/storage.go	(*SQLStore).registry	const:tenantRegistryReason	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/activity_sql.go	(*SQLActivityStore).Get	literal	login_activity has no tenant_id: read on sign-in, before a tenant is chosen
internal/store/activity_sql.go	(*SQLActivityStore).ImportActivity	literal	boot import replay into login_activity, runs before the first request
internal/store/activity_sql.go	(*SQLActivityStore).Touch	literal	login_activity has no tenant_id: one row per person, shared by every house
internal/store/announcement_read_sql.go	(*SQLAnnouncementReadStore).ImportReads	literal	boot import replay of the JSON read-marker snapshot: it spans every tenant and runs before the first request
internal/store/announcement_sql.go	(*SQLAnnouncementStore).ImportAnnouncements	literal	boot import replay of the JSON announcement snapshot: it spans every tenant and runs before the first request
internal/store/attachment_sql.go	(*SQLAttachmentStore).ImportAttachments	literal	boot import replay of the JSON attachment snapshot: it spans every tenant and runs before the first request
internal/store/attachment_sql.go	(*SQLAttachmentStore).PurgeDeletedBefore	literal	retention purge sweeps every tenant's attachments: the scheduler that calls it has no tenant in scope
internal/store/contact_sql.go	(*SQLContactBookStore).ImportContacts	literal	boot import replay of the JSON contact snapshot: it spans every tenant and runs before the first request
internal/store/document_sql.go	(*SQLDocumentStore).ImportDocuments	literal	boot import replay of the JSON document snapshot: it spans every tenant and runs before the first request
internal/store/event_sql.go	(*SQLEventStore).ImportEvents	literal	boot import replay of the JSON event snapshot: it spans every tenant and runs before the first request
internal/store/handover_sql.go	(*SQLHandoverStore).ConfirmByToken	literal	handover confirmation link: the token is the only identifier the recipient has, and it names no tenant
internal/store/handover_sql.go	(*SQLHandoverStore).GetByToken	literal	handover confirmation link: the token is the only identifier the recipient has, and it names no tenant
internal/store/handover_sql.go	(*SQLHandoverStore).ImportHandovers	literal	boot import replay of the JSON handover snapshot: it spans every tenant and runs before the first request
internal/store/home_connector.go	(*SQLHomeConnectorStore).ExchangePairing	literal	the connector presents a pairing secret, not a tenant: the row is found by hash
internal/store/home_connector.go	(*SQLHomeConnectorStore).Heartbeat	literal	the connector presents a credential hash, not a tenant: heartbeats arrive with no request context to take a tenant from
internal/store/home_connector.go	(*SQLHomeConnectorStore).StartPairing	const:slugRegistryReason	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/home_connector.go	existingTenantRef	const:slugRegistryReason	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/home_connector_reading.go	(*SQLHomeConnectorReadingStore).Upsert	const:slugRegistryReason	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/home_portal.go	(*SQLHomePortalStore).Activate	literal	the home portal activation path is addressed by slug, not by a TenantRef, so there is no tenant reference to scope to
internal/store/home_portal.go	(*SQLHomePortalStore).Get	literal	the home portal read path is addressed by slug, not by a TenantRef, so there is no tenant reference to scope to
internal/store/home_portal.go	(*SQLHomePortalStore).ListByOwner	literal	cross-tenant by design: this answers which houses one owner has, so scoping it to any single one would hide the rest
internal/store/home_reservation.go	(*SQLHomeReservationStore).Confirm	literal	the home reservation confirmation path is addressed by slug, not by a TenantRef, so there is no tenant reference to scope to
internal/store/home_reservation.go	(*SQLHomeReservationStore).Get	literal	the home reservation read path is addressed by slug, not by a TenantRef, so there is no tenant reference to scope to
internal/store/home_reservation.go	(*SQLHomeReservationStore).PurgePendingBefore	literal	retention sweep over every tenant's expired reservations: the boot path that calls it has no tenant in scope
internal/store/home_reservation.go	(*SQLHomeReservationStore).Reserve	literal	onboarding reserves a slug that has no tenant row yet, so there is nothing to scope to
internal/store/identity_profile.go	(*SQLIdentityStore).MutateTenantPermissions	literal	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/identity_profile.go	(*SQLIdentityStore).Update	literal	a UserProfile spans every house the person belongs to, and the write touches persons, which has no tenant_id
internal/store/identity_profile.go	(*SQLIdentityStore).writeProfile	literal	a UserProfile spans every house the person belongs to, and the write touches persons, which has no tenant_id
internal/store/identity_sql.go	(*SQLIdentityStore).ChangePersonEmail	literal	persons has no tenant_id: an email change follows the identity into every house at once
internal/store/identity_sql.go	(*SQLIdentityStore).DeletePerson	literal	persons has no tenant_id and this delete cascades into every house's membership: a platform-level act that must NOT be scoped, because a tenant lane here was measured deleting another tenant's membership through the cascade
internal/store/identity_sql.go	(*SQLIdentityStore).ListPersons	literal	persons has no tenant_id: this is the platform-wide people list, not one house's
internal/store/identity_sql.go	(*SQLIdentityStore).MembershipsForPerson	literal	cross-tenant by design: this answers which houses a person is in, so scoping it to one would answer a different question
internal/store/identity_sql.go	(*SQLIdentityStore).PersonByEmail	literal	persons has no tenant_id: one identity per email, shared by every house it belongs to
internal/store/identity_sql.go	(*SQLIdentityStore).PersonByID	literal	persons has no tenant_id: one identity per email, shared by every house it belongs to
internal/store/identity_sql.go	(*SQLIdentityStore).SetMembership	literal	membership house identity is resolved or minted in the global tenant registry before entering its tenant lane
internal/store/identity_sql.go	(*SQLIdentityStore).UpsertPerson	literal	persons has no tenant_id: minting or updating an identity is a platform-level act
internal/store/identity_sql.go	(*SQLIdentityStore).membershipBySlug	literal	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/identity_sql.go	(*SQLIdentityStore).removeMembershipBySlug	literal	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/index_reference.go	(*IndexReferenceStore).Apply	literal	VPI system importer writes global Statistics Austria reference data and revision audit
internal/store/issue_sql.go	(*SQLIssueStore).ImportIssues	literal	boot import replay of the JSON issue snapshot: it spans every tenant and runs before the first request
internal/store/notification_pref_sql.go	(*SQLNotificationPrefStore).Get	literal	notification_prefs has no tenant_id: a person has one mail preference set, not one per house
internal/store/notification_pref_sql.go	(*SQLNotificationPrefStore).ImportPrefs	literal	boot import replay of the JSON preference snapshot into a table with no tenant_id, before the first request
internal/store/notification_pref_sql.go	(*SQLNotificationPrefStore).Set	literal	notification_prefs has no tenant_id: a person has one mail preference set, not one per house
internal/store/organisation_membership_service.go	(*OrganisationMembershipService).change	literal	organisation membership transaction spans the selected organisation's stored house set and its origin-owned undo records, member row and durable audit
internal/store/parking_sql.go	(*SQLParkingStore).ImportParking	literal	boot import replay of the JSON parking snapshot: it spans every tenant and runs before the first request
internal/store/parking_sql.go	(*SQLParkingStore).tenantRef	literal	slug-to-tenant_id resolution reads the tenant registry, before there is an identity to scope to
internal/store/person_avatar_sql.go	(*SQLPersonAvatarStore).Avatar	const:personAvatarLane	person_avatars has no tenant_id: the profile picture follows the person across every house
internal/store/person_avatar_sql.go	(*SQLPersonAvatarStore).AvatarByID	const:personAvatarLane	person_avatars has no tenant_id: the profile picture follows the person across every house
internal/store/person_avatar_sql.go	(*SQLPersonAvatarStore).DeleteAvatar	const:personAvatarLane	person_avatars has no tenant_id: the profile picture follows the person across every house
internal/store/person_avatar_sql.go	(*SQLPersonAvatarStore).SetAvatar	const:personAvatarLane	person_avatars has no tenant_id: the profile picture follows the person across every house
internal/store/profile_overlay_sql.go	(*SQLProfileOverlayStore).Get	literal	profile_overlays has no tenant_id: the self-service name and phone follow the person across every house
internal/store/profile_overlay_sql.go	(*SQLProfileOverlayStore).ImportOverlays	literal	boot import replay of the JSON overlay snapshot into a table with no tenant_id, before the first request
internal/store/profile_overlay_sql.go	(*SQLProfileOverlayStore).upsert	literal	profile_overlays has no tenant_id: the self-service name and phone follow the person across every house
internal/store/scoped_db.go	(*TenantDB).Unscoped	dynamic	reason
internal/store/telegram_sql.go	(*SQLTelegramStore).ChatsByEmail	literal	telegram_links has no tenant_id: every chat a person has, whichever house the notification came from
internal/store/telegram_sql.go	(*SQLTelegramStore).ConsumeLinkCode	literal	redemption spans telegram_link_codes and telegram_links, neither of which has a tenant_id
internal/store/telegram_sql.go	(*SQLTelegramStore).CreateLinkCode	literal	telegram_link_codes has no tenant_id: a code is issued to a person, redeemable from any house they belong to
internal/store/telegram_sql.go	(*SQLTelegramStore).ImportTelegram	literal	boot import replay of the JSON telegram snapshot into three tables that have no tenant_id, before the first request
internal/store/telegram_sql.go	(*SQLTelegramStore).LinkByChat	literal	telegram_links has no tenant_id: an incoming chat is resolved before any house is known
internal/store/telegram_sql.go	(*SQLTelegramStore).Links	literal	telegram_links has no tenant_id: a chat is linked to a person, not to a house
internal/store/telegram_sql.go	(*SQLTelegramStore).Offset	literal	telegram_state has no tenant_id: the bot poll offset is one cursor for the whole process
internal/store/telegram_sql.go	(*SQLTelegramStore).SetOffset	literal	telegram_state has no tenant_id: the bot poll offset is one cursor for the whole process
internal/store/telegram_sql.go	(*SQLTelegramStore).Unlink	literal	telegram_links has no tenant_id: unlinking is a chat-level act, not a house-level one
internal/store/unit_payment_status_sql.go	(*SQLUnitPaymentStatusStore).ImportStatuses	literal	boot import replay of the JSON payment-status snapshot: it spans every tenant and runs before the first request
internal/store/unit_sql.go	(*SQLUnitStore).ImportUnits	literal	boot import replay of the JSON unit snapshot: it spans every tenant and runs before the first request
internal/store/vote_sql.go	(*SQLVoteStore).ImportBallots	literal	boot import replay of the JSON ballot snapshot: it spans every tenant and runs before the first request
