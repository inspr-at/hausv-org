{
  "name": "hausv-snapshot",
  "private": true,
  "type": "module",
  "description": "Visual + HTML regression harness. Uses the system Chromium; never downloads a browser.",
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
