{
  "name": "hausv-snapshot",
  "private": true,
  "type": "module",
  "description": "Visual + HTML regression harness. Uses system Chromium locally and pinned Playwright Chromium in CI.",
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
