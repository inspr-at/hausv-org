# Exclude Claude command symlinks from git archive (deploy blocker fix)
# These symlinks target doctrine/doctrine-private submodule content
# that is not included in the archive, causing tar extraction failures.
.claude/** export-ignore
internal/scripts/.claude/** export-ignore
