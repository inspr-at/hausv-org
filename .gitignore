.DS_Store
# Compiled binary. `go build` names it after the module (hausv-org); the
# Dockerfile still emits weg-portal until the deploy rename lands.
hausv-org
weg-portal
*.log
*.env
*.env.local
local-*.jpg
tmp/

# snapshot harness deps (system Chromium is used; no browser is downloaded)
scripts/snapshot/node_modules/
scripts/snapshot/package-lock.json
